import { test, expect, type Page } from '@playwright/test';
async function login(page:Page,email:string){await page.goto('/login');await page.getByLabel('Email',{exact:true}).fill(email);await page.getByLabel('Password',{exact:true}).fill('P4ssword@123');await page.getByRole('button',{name:'Sign in'}).click();await expect(page).toHaveURL(/dashboard/)}
async function navigateFromMenu(page:Page,name:string){await page.getByRole('link',{name,exact:true}).click();}
test('student submits a physical request',async({page})=>{await login(page,'student@library.demo');await navigateFromMenu(page,'Catalog');await page.locator('article').filter({hasText:'Community Research Methods'}).getByRole('link',{name:/View material/}).click();await page.getByRole('button',{name:'Borrow physical copy'}).click();await page.getByRole('button',{name:'Submit request'}).click();await expect(page.getByRole('status')).toContainText('submitted');});
test('administrator approves and returns a physical request',async({page})=>{await login(page,'student@library.demo');await navigateFromMenu(page,'Catalog');await page.locator('article').filter({hasText:'Community Research Methods'}).getByRole('link',{name:/View material/}).click();await page.getByRole('button',{name:'Borrow physical copy'}).click();await page.getByRole('button',{name:'Submit request'}).click();await page.getByRole('button',{name:'Open account menu'}).click();await page.getByRole('button',{name:'Sign out'}).click();await login(page,'superadmin@library.demo');await navigateFromMenu(page,'Requests');await page.getByRole('button',{name:/Approve Community Research Methods/}).click();await page.getByRole('button',{name:'Confirm approved'}).click();await page.getByRole('button',{name:/Return Community Research Methods/}).click();await page.getByRole('button',{name:'Confirm returned'}).click();await expect(page.getByText('returned',{exact:true}).first()).toBeVisible();});
